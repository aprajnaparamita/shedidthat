{
  "name": "she-absolutely-just-did-that-api",
  "version": "1.0.0",
  "description": "Jess — the post-sex debrief chatbot backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:sentry": "node -r dotenv/config tools/test_sentry.js",
    "test": "jest --detectOpenHandles"
  },
  "dependencies": {
    "hono": "^3.11.7",
    "toucan-js": "^4.1.1"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "jest": "^29.7.0",
    "node-fetch": "^2.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
